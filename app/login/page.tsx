'use client';
import Image from 'next/image';
import { FormEvent, useState } from 'react';
import { Eye, EyeOff, LockKeyhole, Mail } from 'lucide-react';
export default function LoginPage(){const [error,setError]=useState('');const [loading,setLoading]=useState(false);const [show,setShow]=useState(false);async function login(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const form=new FormData(e.currentTarget);try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.get('email'),password:form.get('password')})});const body=await r.text();let json:{error?:string}={};try{json=body?JSON.parse(body):{};}catch{}if(r.ok)window.location.assign('/');else setError(json.error || `Sign-in failed (${r.status}). Please try again.`);}catch{setError('Unable to reach the server. Check your connection and try again.')}finally{setLoading(false)}}return <main className="login"><section className="login-visual"><Image src="/images/logo.webp" alt="Veg Basket" width={166} height={183} priority/><div><p>UAE VEGETABLE TRADING ERP</p><h1>Fresh produce.<br/>Sharper business.</h1><span>Everything Veg Basket needs to trade with confidence.</span></div></section><section className="login-form"><div className="login-card"><Image src="/images/logo.webp" alt="Veg Basket" width={82} height={90} priority/><h2>Welcome back</h2><p>Sign in to manage your business.</p><form onSubmit={login}><label>Email address<div><Mail size={16}/><input name="email" type="email" required autoComplete="email" placeholder="name@vegbasket.ae"/></div></label><label>Password<div><LockKeyhole size={16}/><input name="password" type={show?'text':'password'} required minLength={8} autoComplete="current-password" placeholder="Enter your password"/><button aria-label={show?'Hide password':'Show password'} type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={16}/>:<Eye size={16}/>}</button></div></label>{error&&<div className="form-error" role="alert">{error}</div>}<button className="login-submit" disabled={loading}>{loading?'Signing in…':'Sign in'}</button></form><small>Secure access for authorized Veg Basket staff.</small></div></section></main>}
