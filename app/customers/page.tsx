// @ts-nocheck
'use client';import{useEffect,useState}from'react';import{Eye,Pencil,Plus,Trash2,WalletCards,X}from'lucide-react';import{FinancialDocument,money}from'@/components/financial-documents';
const today=new Date().toISOString().slice(0,10);
export default function Customers(){const[rows,setRows]=useState([]),[form,setForm]=useState(undefined),[ledger,setLedger]=useState(null),[remove,setRemove]=useState(null),[selected,setSelected]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);const load=()=>fetch('/api/customers').then(r=>r.json()).then(x=>Array.isArray(x)&&setRows(x));useEffect(()=>{void load()},[]);const get=id=>fetch('/api/customers/'+id).then(r=>r.ok?r.json():null),open=async id=>{setError('');setSelected('');setLedger(await get(id))},sale=ledger?.sales.find(x=>x.id===selected),due=sale?+sale.total-+sale.paid:0;async function save(e){e.preventDefault();setBusy(true);const d=new FormData(e.currentTarget),r=await fetch(form?.id?'/api/customers/'+form.id:'/api/customers',{method:form?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(d))});setBusy(false);if(r.ok){setForm(undefined);load()}else setError((await r.json()).error)}async function pay(e){e.preventDefault();if(!ledger||!sale)return;const d=new FormData(e.currentTarget),amount=Number(d.get('amount'));if(!Number.isFinite(amount)||amount<=0||amount>due)return setError('Payment must be greater than AED 0.00 and cannot exceed the invoice balance.');setBusy(true);const r=await fetch('/api/customers/'+ledger.customer.id+'/payments',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({saleId:selected,date:d.get('date'),amount,method:d.get('method'),notes:d.get('notes')})});setBusy(false);if(r.ok){setLedger(await get(ledger.customer.id));setSelected('');load()}else setError((await r.json()).error)}return <main className="management"><header className="management-head"><div><p className="eyebrow">CUSTOMER MANAGEMENT</p><h1>Customers</h1><p>Manage customers, sales balances, and collections.</p></div><button className="primary" onClick={()=>setForm(null)}><Plus size={17}/> Add customer</button></header><section className="card data-panel"><div className="supplier-row labels"><span>Customer</span><span>Mobile</span><span>Sales</span><span>Paid</span><span>Outstanding</span><span>Actions</span></div>{rows.map(c=><div className="supplier-row" key={c.id}><b>{c.name}</b><span>{c.mobile}</span><span>{money(c.totalSales)}</span><span>{money(c.totalPaid)}</span><b className={c.outstanding?'payable':''}>{money(c.outstanding)}</b><span className="row-actions"><button title="View statement" onClick={async()=>setLedger({...await get(c.id),statement:true})}><Eye size={16}/></button><button title="Edit" onClick={()=>setForm(c)}><Pencil size={16}/></button><button className="danger" title="Delete" onClick={()=>setRemove(c)}><Trash2 size={16}/></button><button title="Customer ledger / collect payment" onClick={()=>open(c.id)}><WalletCards size={16}/></button></span></div>)}</section>{form!==undefined&&<div className="modal"><div className="modal-backdrop"/><section className="supplier-form card"><button className="sheet-close" onClick={()=>setForm(undefined)}><X/></button><h2>{form?'Edit customer':'Add customer'}</h2><form onSubmit={save}><label>Customer name<input name="name" required defaultValue={form?.name}/></label><label>Mobile<input name="mobile" required defaultValue={form?.mobile}/></label><label>Address<textarea name="address" defaultValue={form?.address}/></label><label>Opening balance (AED)<input name="openingBalance" type="number" min="0" step=".01" defaultValue={form?.openingBalance||'0'} required/></label>{error&&<div className="form-error">{error}</div>}<button className="primary" disabled={busy}>{busy?'Saving…':form?'Update customer':'Save customer'}</button></form></section></div>}{remove&&<div className="modal"><div className="modal-backdrop"/><section className="supplier-form card"><h2>Delete customer?</h2><p>{remove.name} can only be deleted when it has no sales history.</p><div className="confirm-actions"><button className="outline" onClick={()=>setRemove(null)}>Cancel</button><button className="delete-button" onClick={async()=>{const r=await fetch('/api/customers/'+remove.id,{method:'DELETE'});if(r.ok){setRemove(null);load()}else setError((await r.json()).error)}}>Delete</button></div></section></div>}{ledger&&(ledger.statement?<Statement ledger={ledger} close={()=>setLedger(null)}/>:<CustomerLedger ledger={ledger} close={()=>setLedger(null)} selected={selected} setSelected={setSelected} sale={sale} due={due} pay={pay} error={error} busy={busy}/>)}</main>}
function CustomerLedger({ledger,close,selected,setSelected,sale,due,pay,error,busy}){const s=ledger.summary;return <div className="modal ledger-modal"><div className="modal-backdrop" onClick={close}/><section className="ledger card"><button className="sheet-close" onClick={close}><X/></button><div className="card-head"><div><p className="eyebrow">CUSTOMER LEDGER</p><h2>{ledger.customer.name}</h2><p>{ledger.customer.mobile}</p></div></div><div className="ledger-totals"><span>Opening<b>{money(s.openingBalance)}</b></span><span>Sales<b>{money(s.totalSales)}</b></span><span>Payments<b>{money(s.totalPaid)}</b></span><span>Outstanding<b>{money(s.outstanding)}</b></span></div>{ledger.sales.map(x=><div className="ledger-line" key={x.id}><span><b>{x.invoiceNumber}</b><small>{x.saleDate}</small></span><span>{money(x.total)}</span><span>Paid {money(x.paid)}</span><b>{money(+x.total-+x.paid)}</b></div>)}<h3>Record payment</h3><form className="pay-form" onSubmit={pay}><select name="saleId" required value={selected} onChange={e=>setSelected(e.target.value)}><option value="">Select sales invoice</option>{ledger.sales.filter(x=>+x.paid<+x.total).map(x=><option key={x.id} value={x.id}>{x.invoiceNumber} · outstanding {money(+x.total-+x.paid)}</option>)}</select>{sale&&<p className="payment-hint">Invoice {sale.invoiceNumber} · Total {money(sale.total)} · Paid {money(sale.paid)} · Outstanding <b>{money(due)}</b></p>}<input name="date" type="date" max={today} defaultValue={today} required/><input name="amount" type="number" min=".01" max={due||undefined} step=".01" required disabled={!sale} placeholder="Amount"/><select name="method"><option>Cash</option><option>Card</option><option>Bank Transfer</option></select><input name="notes" placeholder="Notes"/>{error&&<div className="form-error">{error}</div>}<button className="primary" disabled={busy||!sale}>{busy?'Processing…':'Record payment'}</button></form></section></div>}
function Statement({ledger,close}){let b=+ledger.summary.openingBalance,e=b?[{date:'—',type:'Opening balance',reference:'Opening',description:'Opening customer balance',purchase:b,payment:0,balance:b}]:[];ledger.sales.forEach(x=>{b+=+x.total;e.push({date:x.saleDate,type:'Sale',reference:x.invoiceNumber,description:'Sales invoice',purchase:+x.total,payment:0,balance:b,status:+x.paid>=+x.total?'Paid':+x.paid?'Partial':'Unpaid'})});return <div className="modal document-modal"><div className="modal-backdrop" onClick={close}/><section className="document-modal-card card"><button className="sheet-close no-print" onClick={close}><X/></button><div className="document-actions no-print"><button className="outline" onClick={()=>window.print()}>Print statement</button></div><FinancialDocument kind="Customer Statement" title={'Customer statement · '+ledger.customer.name} party={ledger.customer.name} contact={ledger.customer.mobile} address={ledger.customer.address} summary={[['Opening Balance',money(ledger.summary.openingBalance)],['Total Sales',money(ledger.summary.totalSales)],['Total Payments',money(ledger.summary.totalPaid)],['Outstanding Balance',money(ledger.summary.outstanding)]]} entries={e}/></section></div>}
