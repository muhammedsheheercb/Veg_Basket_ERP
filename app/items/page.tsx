'use client';
import { FormEvent, useEffect, useState } from 'react';
import { Plus, Pencil, Trash2, X } from 'lucide-react';
import { ListFilters, ListPagination, useListControls } from '@/components/list-controls';
type I={id:string;code:string;name:string;description?:string;active:boolean};
export default function Items(){const[rows,setRows]=useState<I[]>([]),[form,setForm]=useState<I|null|undefined>(),[error,setError]=useState('');const load=()=>fetch('/api/items').then(r=>r.json()).then(setRows);useEffect(()=>{void load()},[]);const controls=useListControls(rows,x=>`${x.code} ${x.name} ${x.description||''}`);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget),r=await fetch(form?`/api/items/${form.id}`:'/api/items',{method:form?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:d.get('name'),description:d.get('description'),active:d.get('active')==='on'})});if(r.ok){setForm(undefined);load()}else setError((await r.json()).error)}return <main className="management"><header className="management-head"><div><p className="eyebrow">ITEM MANAGEMENT</p><h1>Items</h1><p>Reusable sale items and item codes.</p></div><button className="primary" onClick={()=>{setError('');setForm(null)}}><Plus size={16}/> Add item</button></header><section className="card data-panel"><ListFilters controls={controls} placeholder="Search item code, name, or description…"/><div className="supplier-row item-row labels"><span>Code</span><span>Item</span><span>Description</span><span>Status</span><span></span><span>Actions</span></div>{controls.pageRows.map(x=><div className="supplier-row item-row" key={x.id}><b>{x.code}</b><span>{x.name}</span><span>{x.description||'—'}</span><span>{x.active?'Active':'Inactive'}</span><span/><span className="row-actions"><button onClick={()=>setForm(x)}><Pencil size={16}/></button><button className="danger" onClick={async()=>{await fetch(`/api/items/${x.id}`,{method:'DELETE'});load()}}><Trash2 size={16}/></button></span></div>)}{!controls.filtered.length&&<p className="empty">No items match these filters.</p>}<div className="list-footer-pagination"><ListPagination controls={controls}/></div></section>{form!==undefined&&<div className="modal"><div className="modal-backdrop" onClick={()=>setForm(undefined)}/><section className="supplier-form item-form card"><button className="sheet-close" onClick={()=>setForm(undefined)}><X/></button><h2>{form?'Edit item':'Add item'}</h2><form onSubmit={save}><label>Item code<input value={form?.code||'Generated automatically when saved'} disabled/><small style={{color:'var(--muted)',fontSize:'11px'}}>Unique system code. It cannot be edited.</small></label><label>Item name<input name="name" required defaultValue={form?.name}/></label><label>Description<textarea name="description" defaultValue={form?.description}/></label><label><input name="active" type="checkbox" defaultChecked={form?.active??true}/> Active</label>{error&&<div className="form-error">{error}</div>}<button className="primary">{form?'Update item':'Save item'}</button></form></section></div>}</main>}
